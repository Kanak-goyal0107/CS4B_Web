const name=prompt("Enter your name");
const age=prompt("Enter your age"); //taking input from browser
const info=`My name is ${name} and age is ${age}`;
console.log(info);  //print on console
document.write(info); //print on browser